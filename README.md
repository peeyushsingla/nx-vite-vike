# NX + React + TypeScript + Vite + vike

- npx nx run my-app:serve
    - start server
    - render server sided generated html
- npx nx run my-app:build
    - generate build
    - generate JS files no HTML files are generated